# {agent_name} Agent

## Installation

```bash
# Quick install (interactive)
npx xiaoma-cli agent-install --source ./{agent_filename}.agent.yaml

# Quick install (non-interactive)
npx xiaoma-cli agent-install --source ./{agent_filename}.agent.yaml --defaults
```

## About This Agent

{agent_description}

_Generated with XIAOMA Builder workflow_
