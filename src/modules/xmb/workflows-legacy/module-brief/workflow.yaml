# Module Brief Workflow Configuration
name: module-brief
description: "Create a comprehensive Module Brief that serves as the blueprint for building new XIAOMA modules using strategic analysis and creative vision"
author: "XiaoMa Builder"

# Critical variables
config_source: "{project-root}/{xiaoma_folder}/xmb/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Reference examples and documentation
existing_modules_dir: "{project-root}/{xiaoma_folder}/"
module_structure_guide: "{project-root}/{xiaoma_folder}/xmb/workflows/create-module/module-structure.md"

# Optional user inputs - discovered if they exist
input_file_patterns:
  brainstorming:
    description: "Brainstorming session outputs (optional)"
    whole: "{output_folder}/brainstorming-*.md"
    load_strategy: "FULL_LOAD"

# Module path and component files
installed_path: "{project-root}/{xiaoma_folder}/xmb/workflows/module-brief"
template: "{installed_path}/template.md"
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"

# Output configuration
default_output_file: "{output_folder}/module-brief-{{module_code}}-{{date}}.md"

standalone: true

# Web bundle configuration
web_bundle: false # XMB workflows run locally in XIAOMA-CLI project
